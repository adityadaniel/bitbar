fastlane_version "2.26.1"
default_platform :mac

platform :mac do
  before_all do
    cocoapods repo_update: true
    # update_project_codesigning(
    #   path: "BitBar.xcodeproj",
    #   use_automatic_signing: true,
    #   team_id: "8Z44P9V4VF"
    # )
  end

  desc "Runs all the tests"
  lane :test do
    scan scheme: "BitBar", clean: true
  end

  lane :key do
    # security import $(CERT) -A -k $(KEYCHAIN) -P "$(CERTPWD)" -T /usr/bin/codesign
    # security import Resources/bitbar.cer -A -k $(KEYCHAIN) -T /usr/bin/codesign

    create_keychain(name: "travis", password: "travis", default_keychain: true, unlock: true, timeout: 3600, add_to_search_list: true)
     import_certificate(certificate_path: "Resources/bitbar.cer", keychain_name: "travis")
     import_certificate(certificate_path: "bitbar.p12", certificate_password: ENV["CERTPWD"], keychain_name: "travis")
  end

  desc "Deploy a new version to the App Store"
  lane :build do
    gym
  end

  lane :lint do
    swiftlint(
  mode: :lint,      # SwiftLint mode: :lint (default) or :autocorrect
)
  end

  lane :fix do
    swiftlint mode: :autocorrect
  end

  # lane :release do
  #   gym scheme: "BitBar"
  #   deliver force: true
  # end
  # lane :build do
  #   gym scheme: "BitBar"
  #   deliver force: true
  # end

  # You can define as many lanes as you want

  after_all do |lane|
    # This block is called, only if the executed lane was successful

    # slack(
    #   message: "Successfully deployed new App Update."
    # )
  end

  error do |lane, exception|
    # slack(
    #   message: exception.message,
    #   success: false
    # )
  end
end
